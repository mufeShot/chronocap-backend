NODE_ENV=development
# Local Postgres (adjust if using Docker / different db name)
DATABASE_URL="postgresql://postgres:admin@localhost:5432/chronocap?schema=public"

# Auth
JWT_SECRET="dev_supersecret_change_me"
JWT_EXPIRES="15m"

# API
PORT=3000
FRONTEND_ORIGIN="http://localhost:5173" # comma separated, no trailing slashes

# Storage driver: local | supabase
STORAGE_DRIVER=local
PUBLIC_BASE_URL="http://localhost:3000"

# Supabase (optional in dev, fill if testing remote storage)
SUPABASE_PROJECT_REF=""
SUPABASE_URL="https://<project-ref>.supabase.co"
SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""
SUPABASE_DB_PASSWORD="${SUPABASE_DB_PASSWORD}"
SUPABASE_STORAGE_BUCKET="Chronocap-Files"

# Real email testing (development)
RESEND_API_KEY="re_N8XbWyb5_EZpueJDmGcfU1ZZKDdcXAAA3"
MAIL_FROM="Chronocap Dev <onboarding@resend.dev>"
